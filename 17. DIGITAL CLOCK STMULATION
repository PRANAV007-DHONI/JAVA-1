#include <stdio.h>
#include <unistd.h> // for sleep()

int main() {
    int h = 0, m = 0, s = 0;
    while(1) {
        printf("\r%02d:%02d:%02d", h, m, s);
        fflush(stdout);
        s++;
        if(s == 60) { m++; s = 0; }
        if(m == 60) { h++; m = 0; }
        if(h == 24) { h = 0; }
        sleep(1);
    }
    return 0;
}
